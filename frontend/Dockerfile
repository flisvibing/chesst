# Frontend served as static files. For local dev use `python -m http.server 5500`
# or any static server. For production, Netlify serves the folder directly.
FROM nginx:alpine
COPY . /usr/share/nginx/html
EXPOSE 80
